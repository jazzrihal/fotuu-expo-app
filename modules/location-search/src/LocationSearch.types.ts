export type SearchSuggestion = {
  title: string;
  subtitle: string;
};

export type ResolvedLocation = {
  latitude: number;
  longitude: number;
  name: string;
};

export type SearchRegion = {
  latitude: number;
  longitude: number;
};
